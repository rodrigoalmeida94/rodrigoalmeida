+++
title = "Blog"
# Blog hidden for now: posts stay in the repo but are neither rendered nor listed.
# Delete this file (or the _build/cascade blocks) to bring the blog back.
[_build]
  render = "never"
  list = "never"
[cascade]
  [cascade._build]
    render = "never"
    list = "never"
+++
